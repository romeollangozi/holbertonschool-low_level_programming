#include "main.h"
/**
 * mul - multiples two given arguments and returns the result
 * @a: first number
 * @b: second number
 * Return: returns the result of the multiplication of the
 * two given arguments
 *
 */
	int mul(int a, int b)
	{
		return (a * b);
	}
